import { ToppingState } from './topping-state.model';

describe('ToppingState', () => {
    it('should compile', () => {
        const model: ToppingState = {} as any;
        expect(model).toBeTruthy();
    });
});
